// Placeholder App component. Real dashboard wiring lands in commit 5+.
// Keep this minimal so the Vite build pipeline can be verified in
// isolation (commit 2 of the migration).
export function App() {
  return (
    <main>
      <h1>Compensation dashboard</h1>
      <p>Vite build pipeline online. Components land in commit 5.</p>
    </main>
  );
}
